{
  "name": "ai-experiment-consultant",
  "version": "0.0.1",
  "private": true,
  "description": "AI-assisted experimentation consultant for UpGrade adoption (PELE 2026 prototype)",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,client -c blue,magenta \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w client",
    "preview": "npm run preview -w client",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
